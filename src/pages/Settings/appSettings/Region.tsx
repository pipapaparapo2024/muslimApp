import React from "react";
import { PageWrapper } from "../../../shared/PageWrapper";

export const Region: React.FC = () => {
  return (
    <PageWrapper showBackButton>
      <div>Region Settings Page</div>
    </PageWrapper>
  );
};