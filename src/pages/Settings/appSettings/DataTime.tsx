import React from "react";
import { PageWrapper } from "../../../shared/PageWrapper";

export const DataTime: React.FC = () => {
  return (
    <PageWrapper showBackButton>
      <div>DataTime Settings Page</div>
    </PageWrapper>
  );
};